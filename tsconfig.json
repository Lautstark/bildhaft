{
  "extends": "@lautstark/toolchain/tsconfig.json",
  "compilerOptions": {
    "types": ["vite/client", "node"]
  },
  "//": "The suites are checked too, not just src. They were outside this list until 2026-08-28, so `npm run typecheck` never read a line of them and vitest and Playwright only ever transpiled them. The compiler options the four products had written out identically are @lautstark/toolchain's now.",
  "include": ["src", "tests", "e2e"]
}
